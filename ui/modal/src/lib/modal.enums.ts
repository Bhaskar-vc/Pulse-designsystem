export enum ModalSize {
  DEFAULT = 'default',
  WIDE    = 'wide',
}

export enum ModalActionsAlign {
  BETWEEN = 'between',
  RIGHT   = 'right',
  CENTER  = 'center',
  STACKED = 'stacked',
}

export enum ModalIntent {
  NONE    = 'none',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR   = 'error',
  INFO    = 'info',
}

export enum ModalLayout {
  STACKED    = 'stacked',
  HORIZONTAL = 'horizontal',
}

export enum ModalVariant {
  MODAL  = 'modal',
  DRAWER = 'drawer',
}
