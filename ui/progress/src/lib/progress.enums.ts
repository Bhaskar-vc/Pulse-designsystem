export enum ProgressColor {
  PRIMARY = 'primary',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR   = 'error',
  INFO    = 'info',
}

export enum ProgressSize {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
}
