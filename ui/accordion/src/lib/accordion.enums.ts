export enum AccordionVariant {
  DEFAULT    = 'default',
  BORDERED   = 'bordered',
  FLUSH      = 'flush',
  STATUS     = 'status',
  STATUS_GAP = 'status-gap',
}

export enum AccordionSize {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
}
