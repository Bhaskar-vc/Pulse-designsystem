import { VcAutocompleteDirective } from './autocomplete.directive';

describe('VcAutocompleteDirective', () => {
  it('should create an instance', () => {
    // const directive = new VcAutocompleteDirective();
    // expect(directive).toBeTruthy();
  });
});
